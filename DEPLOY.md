# 放在哪儿跑，和为什么

herdr-web 后面是**一个登录 shell**（发件箱那条路即使不开终端也能让 agent 跑命令）。所以
「放在哪儿」这件事值得花五分钟。这篇讲三件事：每道防线到底在挡什么、有哪几档配置、
为了省事可以砍掉什么和砍掉之后会怎样。

## 一分钟版

- **只在这台机器上用** → `./herdr-web`，完事，下面都不用看。
- **手机/平板要连** → 必须有 TLS（不然同一个 Wi-Fi 上抓包就能看到你整个终端），看下面的档位。
- **有自己的域名吗** → 有：浏览器零警告，而且能用 passkey。没有：自签，每台设备第一次点一下
  「继续访问」。

## 每道防线在挡什么

不用记机制名字，看右边这一列就行。

| 机制 | 不做会发生什么 |
|---|---|
| **一台设备配一次**，凭据绑设备不绑 IP | 按 IP 记住信任的话：客人来连 Wi-Fi，路由器把 `192.168.1.23` 分给他 —— 那正是你上周批准过的手机的地址，他打开页面就进了你的 shell |
| **配对码一次性、5 分钟过期** | 你把带二维码的截图发到群里。一小时后有人照着扫 —— 如果码是长期有效的，他就进来了 |
| **服务端只存哈希** | 你让 agent 读了个不可信的网页，它被指使 `cat ~/.herdr-web/devices.json` 然后打到屏幕上。存明文就等于交出凭据 |
| **URL 里不放秘密** | 手机书签同步到 iCloud、或者你截屏发给同事 —— 带 token 的链接就这么漏出去了 |
| **能撤销**（`herdr-web revoke`） | 手机丢了。没有撤销就只能换掉总 token，家里所有设备一起废 |
| **TLS** | 咖啡店 Wi-Fi 上有人抓包：你的登录凭据和整个终端画面（每一次按键）都是明文 |
| **Host 白名单** | 你点开一个陌生网页，它把自己的域名解析到 `192.168.1.5`。浏览器眼里这就是同源，于是替它把你的凭据发过去（DNS rebinding） |
| **限速 + 封锁** | 公网上的扫描器一天试几万次配对码，而你完全不知道有人在试 |
| **声明 `EXPOSED=1`** | 走 frp / 隧道时，herdr-web 看到自己只监听 `127.0.0.1`，于是以为很安全 —— 实际挂在整个互联网上。这个没法自动测出来，只能你告诉它 |
| **passkey** | 手机没锁屏被人拿去用一下，打开书签就是你的 shell。而且没有它，会话凭据只能选「活得久」或「被偷了窗口短」中的一个 |

### passkey 怎么用

在网页的 **设置 → 设备 → passkey → 添加** 加一把（要求当前设备已经登录）。加完之后：

- **换新设备不用回机器前**：新设备打开页面 → 「用 passkey 登录」→ Face ID。同步的 passkey
  （iCloud 钥匙串 / Google 密码管理器）在你所有设备上都有。
- **凭据被偷的窗口从三个月压到一天**：默认每 24 小时要重验一次（`HERDR_WEB_REAUTH_HOURS`，
  设 0 关掉）。这道关卡整个会话一起卡，不只卡终端 —— 因为发件箱本身就是代码执行。
- 配对码仍然留着，作为「passkey 丢了」和「用 IP 访问」时的回去的路。

前提是**用域名访问**。裸 IP 上按钮不会出现（WebAuthn 规定标识必须是域名，`localhost` 是特例）。

## 几档配置

| 档 | 要准备什么 | 每台设备第一次 | passkey |
|---|---|---|---|
| **A 只本机** | 无 | 零步骤 | — |
| **B 局域网 · 用 IP 访问** | 无 | 点一次「继续访问」（或装一次 CA 证书） | 不能用 |
| **C 局域网 · 用自己的域名** | 域名 + DNS 的 API token | 零步骤 | 能用 |
| **D 公网** | 同 C，再加一个公网入口（隧道，或公网 IP） | 零步骤 | 能用 |

**A** 直接 `./herdr-web` 就行。

**B**：

```bash
HERDR_WEB_HOST=0.0.0.0 ./herdr-web        # 自动自签证书，扫终端里的二维码
```

**C**：把域名的 A 记录指到内网地址（`herdr.example.com → 192.168.1.5`），**证书 herdr-web
自己会签**（DNS-01 不需要外网能连进来，所以内网地址也签得出来）。公网上一个端口都不用开。

```bash
HERDR_WEB_HOST=0.0.0.0 \
HERDR_WEB_HOSTNAME=herdr.example.com \
HERDR_WEB_ACME_DNS=cloudflare \
HERDR_WEB_CLOUDFLARE_DNS_API_TOKEN=... \
./herdr-web
```

支持 `cloudflare` / `alidns`（阿里云）/ `tencentcloud`（腾讯云）/ `route53`（AWS）/
`digitalocean` / `huaweicloud` —— **每家的 token 怎么拿、该给什么权限，见 [DNS.md](DNS.md)**。**第一次先加 `HERDR_WEB_ACME_STAGING=1`** 跑通流程 ——
正式环境同一组域名一周只给 5 张证书，反复试会把自己锁一周。签完的证书放在
`~/.herdr-web/data/acme/`，到期前 30 天自动续，续完**不用重启**（热重载）。

已经有证书（`lego` / `acme.sh` / certbot 管的）也行，直接指过去：

```bash
HERDR_WEB_TLS_CERT=/path/fullchain.pem HERDR_WEB_TLS_KEY=/path/privkey.pem ./herdr-web
```

那种也会热重载 —— 你的续期脚本换了文件，十秒内自己捡起来，不用重启。

**D**：家宽在 NAT 后面没有公网入站，所以要么有公网 IP（**先检查有没有公网 IPv6，很多宽带都有，
那就什么隧道都不用**），要么用隧道。隧道分两类，区别不是好不好配，是**这个口在不在公网上**：

| | 口在公网上 | 谁能看到明文 | 手机后台 |
|---|---|---|---|
| **VPN 类**（Tailscale / Headscale / WireGuard） | **不在**，公网扫不到 | 没人（点对点加密） | 系统级 VPN，稳 |
| **穿透类**（frp / Cloudflare Tunnel） | **在** | frp 看你怎么配；Cloudflare Tunnel 是 Cloudflare | — |

走 VPN 类的话，端口扫描、在线猜解、从公网来的 DNS rebinding 这一整类攻击面直接消失。

穿透类要多给两个变量，一个都不能少：

```bash
HERDR_WEB_EXPOSED=1 \                       # 声明「这个口公网能碰到」，见上面那张表
HERDR_WEB_TLS_CERT=... HERDR_WEB_TLS_KEY=... \
HERDR_WEB_HOSTNAME=herdr.example.com \
HERDR_WEB_PUBLIC_URL=https://herdr.example.com:12345 \   # 公网端口和本地端口不一样时必给，
./herdr-web                                              # 不然启动横幅里的二维码是废的
```

前面有反代（nginx / Traefik / Caddy）帮你终止 TLS 的话，把上面两个证书变量换成
`HERDR_WEB_TLS=proxy`，另外只有在**确认公网唯一入口就是那个反代**之后才加
`HERDR_WEB_TRUST_PROXY=1`（否则谁都能自带一个 `X-Forwarded-For` 伪造来源 IP，把限速绕干净）。

## 想省事，能砍什么

| 砍掉 | 省了什么 | 代价 |
|---|---|---|
| 域名（改用 IP + 自签） | 不用买域名、不用配 DNS token | 每台设备第一次要点「继续访问」，而且**永久失去 passkey**（浏览器规定它的标识必须是域名，IP 不行） |
| 反代 | 少一个组件 | 无 —— 而且更安全：反代是会解密你终端画面的那一跳，去掉它就是端到端加密。证书让 `HERDR_WEB_ACME_DNS` 自己签就行 |
| 隧道 | 少一个组件 | 只有在你有公网 IP（含 IPv6）时才能砍，否则外网连不上 |
| TLS（`HERDR_WEB_INSECURE=1`） | 什么都不用准备 | **别这么干**。同网抓包就拿到你的凭据和整个终端。只适合临时调试 |
| 撤销 / 限速 / Host 白名单 | — | 不提供关掉的开关，它们没有摩擦，也没有理由关 |

反过来，**唯一不能砍的**是 `HERDR_WEB_EXPOSED=1`：只要这个口能从公网碰到就必须声明，
因为程序自己测不出来。不声明的话，「看起来只监听本机」这个假象会让它以为可以放松。

## 管理页

`http://127.0.0.1:<端口+1>/`（启动横幅里有，比如主服务 7788 → 管理页 7789）。

看证书状态和到期时间、点一下签发或者「用 staging 试一次」、选服务商生成 `.env` 片段、
出配对码、踢设备、删 passkey。**上次续期失败的完整错误也在这儿** —— 不然它只在终端打一行，
你不盯着终端就不知道，直到某天早上所有设备一起报证书过期。

它**只监听 127.0.0.1，公网上不存在**，所以不需要登录：能连上它的东西已经有你的 shell 了。
想从别的机器看就 `ssh -L 7789:127.0.0.1:7789 那台机器`。

⚠️ 「只绑 loopback」挡住了网络上的别人，但**挡不住浏览器** —— 你在这台机器上打开的任何网页
都能 fetch `http://127.0.0.1:7789/`。所以它照样校验 Host 只认 loopback 字面量（挡 DNS
rebinding）、写操作要自定义头。**loopback-only ≠ 浏览器安全**，这条容易漏。

## 出问题先看这里

- **打开页面提示「Host 不在白名单」（421）** → 用域名访问就得 `HERDR_WEB_HOSTNAME=那个域名`。
- **一直跳回配对页** → 十有八九是 cookie 没带上：`HERDR_WEB_TLS` 的档位和浏览器地址栏里
  的 http/https 不一致（https 下 cookie 会带 `Secure`，走回 http 就发不出来了）。
- **配对码用掉了、人又不在机器前** → 只能回到机器前 `herdr-web pair`。这是故意的：能读到那个
  终端就证明你在机器前，这是目前唯一的带外因子。
- **终端里跳出「xxx 被改过，不是本进程写的」** → 有东西在你背后动了 `data/devices.json` 或
  `data/passkeys.json`。这两个文件**怕写不怕读**：读走没用（一个只有哈希、一个只有公钥），
  但往里加一行就等于给自己发一份能进来的凭据。跑着的服务不会重读它们，所以外部写入当场
  不生效 —— 但要查一下是谁干的（这台机器上的 agent 是以你的身份跑的，写得动）。
- **`herdr-web devices` 报「服务在跑但通道连不上」** → 这是故意拦住的：直接改文件会和服务
  内存里那份打架、静默丢数据。看服务启动日志里 `ctl.sock` 那一行（常见原因是 `HERDR_WEB_DIR`
  指得太深，unix socket 路径有 104 字节上限）。
- **局域网那条「切过去」的横幅根本不出现** → 先确认服务端这侧开着：
  `curl -s -D- -o /dev/null http://127.0.0.1:<主口>/ | grep -i content-security-policy`，
  `connect-src` 里应当有 `https://<内网IP>:<直连口>`。只有 `'self'` 就说明 `HERDR_WEB_LAN_PORT`
  没生效 —— 最常见的是**改了 `.env` 但没重启**。
- **日志里「局域网直连口 … 起不来（不影响主服务）」** → 那个端口被别人占了。主服务照常跑，
  只是没有加速，网页嗅探失败会安静地留在隧道那条路上。
- **明明点过「继续访问」，还是不切** → 浏览器的证书例外是按「域名 + 端口 + **证书**」记的。
  换过 `HERDR_WEB_DIR`、删过 `tls/`、或者当时点的其实是另一个实例（比如临时开在同一个端口上
  的那种），证书就换了一张，例外跟着作废，要再点一次。
- **隧道断了之后自己起不来**（frp 常见）：日志里一直 `i/o deadline reached`，重启一下就好。
  多半是本机代理的 TUN 拦了容器出网 —— 给隧道服务器的 IP 加一条 DIRECT 规则，或者别把隧道跑在
  容器里。

安全机制本身怎么设计的、还有哪些没做，看 [SECURITY.md](docs/dev/SECURITY.md)。

---

## 从公网连（frp / 隧道）

（原来在 README「安全」那节。）

#### 从公网连（frp / 隧道）

> 完整的公网访问方案、分档简化路线、以及实际部署时踩的运维坑：[DEPLOY.md](DEPLOY.md)。

推荐 **frp 的 `type = tcp` + herdr-web 自己拿真证书**：TLS 端到端，frps 那台 VPS 上只看得到密文。用 frp 的 https 模式就是在 VPS 上解密，那台机器能看到你的整个终端画面。

```bash
HERDR_WEB_EXPOSED=1 HERDR_WEB_TLS_CERT=~/certs/herdr.example.com/fullchain.pem HERDR_WEB_TLS_KEY=~/certs/herdr.example.com/privkey.pem HERDR_WEB_HOSTNAME=herdr.example.com HERDR_WEB_PUBLIC_URL=https://herdr.example.com:17788 ./herdr-web
```

证书用 DNS-01 签（`lego` / `certbot` 都行）：**不需要公网可达，只要能改一条 TXT 记录**，所以把 A 记录指到内网地址也能拿到浏览器默认信任的证书 —— 手机上零警告、是 secure context（剪贴板和 `OSC 52` 跟着正常）、passkey 的域名门槛也一起解决了。

⚠️ **`HERDR_WEB_EXPOSED=1` 必须自己声明**：走 frp 时 frpc 从本机连过来，herdr-web 看到的监听地址是 `127.0.0.1`、每个请求的源地址也是 `127.0.0.1`，「是不是本机」这个判据彻底失效。没声明的话「暴露了却检测不到」和「本机免配对把公网请求也放进来」这两个洞会同时开着。前者靠这个变量兜，后者已经改成**默认关**了。

⚠️ **frp 的 tcp 模式拿不到客户端真实 IP**：所有请求在 herdr-web 眼里都来自 `127.0.0.1`（frpc 在容器里时也一样）。这有两个后果：

1. 按 IP 的限速会把所有人算成同一个。封锁只挡新配对、不碰已有会话，所以最坏是「十五分钟内配不了新设备」，`herdr-web unlock` 解开。
2. **「本机永不封」这个豁免必须关掉**，否则整层限速是空转 —— 看起来配了，一次都不生效。`HERDR_WEB_EXPOSED=1` 会自动关掉它，这也是这个变量必须声明的另一个理由。

想要真实 IP 就在 frpc 上开 `transport.proxyProtocolVersion = "v2"`（herdr-web 还得会解 PROXY 协议头，现在不会），或者用 http 模式让 frps 加 `X-Forwarded-For`（那时候才配 `HERDR_WEB_TRUST_PROXY=1`）。**没有可信前置千万别开那个变量** —— 攻击者自带一个头就能伪造源 IP。

## 局域网直连（在家时别绕隧道）

走隧道的时候每个字都是**两跳公网**：设备 → VPS → 家里那台。同一个 Wi-Fi 下这段是 2–5ms，
绕一圈是 60–100ms —— 对终端来说这就是「跟手」和「发飘」的区别。而人在家的时间往往占大多数。

```bash
HERDR_WEB_LAN_PORT=7790     # 其余变量照原样
```

这会在 `0.0.0.0:7790` 上另开一个监听，自签证书、SAN 跟着当前局域网地址走。之后：从公网那个
地址打开页面 → 网页嗅探那几个局域网地址 → 探通了问一次「切过去？」→ 同意之后再遇到就静默切。

**书签不用改，还是公网那个地址。** 只有首屏那一次走隧道，之后终端的 WebSocket 和每一个接口
调用都是直连。

### 每台设备要手动做一次（跳不过去）

**先开一次 `https://<内网IP>:7790/`，点「继续访问」。**

地址不用去终端里抄 —— **网页的「设置 → 终端」页底下有一节「局域网直连」**，里面就是这个
地址（可以直接点开，开新标签页），还标着「正走直连 / 正走公网」，以及一个「探到局域网就
自动切过去」的开关。之所以要在界面里给一份：这个地址原来只印在启动横幅上，而平板上的人
看不到横幅 —— 不知道地址就永远迈不出第一步；而且它会变（DHCP），也不能靠人抄一次。
那个开关同时是横幅上点过「不用」之后**唯一能反悔的地方**。

那张证书是自签的。没点过这一下的话，网页那次嗅探会在 TLS 握手就失败，页面**安静地**留在隧道
那条路上 —— 这是故意的：「证书验不过」和「我此刻在外面」在浏览器眼里长得一模一样，分不出来，
那就宁可不切。

想彻底免掉这一步，就把 `~/.herdr-web/tls/ca.pem` 装到设备上并信任（横幅上有路径和指纹），
那之后换网段也不用再点。

⚠️ 直连那一侧**用不了 passkey**，而且这跟装不装 CA 无关 —— 原因见下面「凭据在直连那一侧会怎样」。

### 为什么这个口非得是 TLS

https 页面对 `http://` 目标的 fetch 一律算 **active mixed content**，浏览器无条件拦死
（`mode:'no-cors'` 也拦，`<img>` 探测会被自动升级成 https 而失败）。Chrome 那个「允许 https
页面访问私有网络」的提案（Private Network Access）从来没落地。

所以「局域网那头用明文 http」不是「安全性差一点」，是**这个功能压根不存在** —— 连嗅探都发不出去。

### 直连那个口只服务本地网络

那个口对**非本地对端**一个字节都不服务：对端地址不是私网 / 链路本地 / 本机的话直接 403，
并在终端日志里告警一次。

这不是多余的保险。「绑通配地址就只有局域网碰得到」是个**拓扑假设**，三种情况下不成立：
路由器上做了端口转发或开了 UPnP；隧道配错把公网流量转到了这个口；以及最隐蔽的那种 ——
`0.0.0.0` 在 Go 里开出来的是**双栈**套接字（`lsof` 里显示成 `*:7790`），而家宽的 IPv6
通常没有 NAT，所以一台有全局 IPv6 地址的机器上，这个口是否公网可达只取决于路由器防火墙。

交接令牌「只能在直连口兑换」这条门槛压在这个口上，所以它不能是一句祈祷。日志里出现那条
告警，说明这个口确实被暴露了 —— 请求已经被拒了，但值得回头看看路由器。

### 内网 IP 会变

**在路由器上给这台机器绑一个固定内网 IP（DHCP 静态租约）。** 这是唯一要你动路由器的地方，
而且只有一次。

不绑也不会坏，只会退化：地址一变就是一个新 origin，你在旧地址上点的那一下「继续访问」跟着
作废，凭据也不在那儿了。herdr-web 认得出这种情况 —— 服务端现在报的地址和上次真的走通过的
那个不是同一个 —— 于是网页上会直接说「局域网地址变成了 x.x.x.x，去开一次并点继续访问」，
而不是从此静默变慢、让你查不出原因。

证书那边是自动的：每半分钟看一次，IP 变了就重签写盘，跑着的监听十秒内热重载，**不用重启**
（重启会断掉所有连着的终端）。

### 凭据在直连那一侧会怎样

这一段是实际用起来最容易意外的地方，因为**换过去就是换了一个 origin**，而 cookie 是
host-only 的 —— 直连那个地址上是一份**独立的凭据**，和公网那条路各归各的。

于是三件事：

**一、切过去那一下凭据是自动带的，你不用手配。** 网页先要一枚**交接令牌**（`/api/handoff`，
只给已认证的会话），跟在 URL 里带过去，落地时换成 cookie 再把 URL 洗干净。

它**不是配对码**（这点很要紧）：配对码那条路「只有坐在机器前的人能出」是一条写进
[SECURITY.md](docs/dev/SECURITY.md) 的性质，网页上不能有任何出码的路径 —— 因为配对码创造的是一份
**不随创造者一起被撤销**的独立凭据，一份被偷的 cookie 就成了无限发凭据的机器。交接令牌在
三处比它窄：60 秒一次性、**只能在直连那个监听上兑换**、兑出来的设备记着上级，**踢掉上级
时一起被踢掉**。详细的取舍在 SECURITY.md §11 的「修正案」。

第二次之后那个 origin 上已经有 cookie 了，令牌就用不上、自己过期，**不会每切一次就多一台设备**。
顺带 `installId` 也带过去（走 fragment，不进服务端日志）：不带的话同一台平板在新 origin 上
算「第一次来」，排布会按设备类别重新猜一套。

**二、设备面板里会多一条。** 同一台平板会出现两次 —— 公网那条路一份、直连一份（后者标签
带「（局域网）」）。这是 cookie 的作用域决定的，不是 bug。注意**撤销是单向级联的**：踢掉公网
那份，它交接出来的直连那份**一起没**；反过来只踢直连那份，公网那份不受影响。

**三、直连那一侧用不了 passkey。** 真正的原因不是证书警告，是 **WebAuthn 的 RPID 只能是域名**，
裸 IP 不是合法标识（`localhost` 是规范里的特例）—— 所以把 `ca.pem` 装到设备上、警告完全消失
之后，passkey 在那一侧照样按不动。

界面上那几个 passkey 按钮在直连那一侧**不会出现**（判据按当前 origin 算，不是按「这个部署配了
RPID 没有」）。一个必然抛 SecurityError 的按钮比没有按钮糟得多 —— 人会以为是自己指纹没录好。

由此来的那条摩擦：`HERDR_WEB_REAUTH_HOURS` 默认 24 小时，而**只要注册过 passkey 这条就生效**
（判据是全局注册数，不是「这个 origin 能不能用」）。于是直连那一侧过了一天会要求重验，而那儿
唯一的出路是面板上的**「重新配对」** —— 要一个配对码（回机器前 `herdr-web pair`）。公网那条路
不受影响，Face ID 照旧。

（**从公网地址再切一次解决不了它**：`?pair=` 落地时服务端先看有没有凭据，有就只把 URL 洗干净、
把那个码丢掉 —— 那条判断是为了「旧书签每打开一次就多一台设备」而写的。所以切过去不会顺手把
重验刷新掉。）

不想要这个摩擦，两条路选一条：

- `HERDR_WEB_REAUTH_HOURS=0` 把重验关掉（那就是拿「凭据被偷的窗口」换省事，代价见上面那张表）；
- 让直连地址是个**域名**而不是 IP：内网 DNS 覆盖，或者在公网 DNS 上加一条指向内网 IP 的 A 记录
  （DNS-01 签证书不需要公网可达，所以这种记录也能拿到真证书）。那时候把 `HERDR_WEB_RPID` 设成
  两个名字的**上级域名**，两个 origin 就共用一套 passkey，这条摩擦整个消失。代价是要碰 DNS，
  而且部分路由器的 DNS rebind 保护会把「公网域名解析出私网地址」的应答丢掉，得给那个域名开白名单。

### 什么时候不用配这个变量

主口本来就是「自签 TLS + 听着局域网」的话（`HERDR_WEB_TLS=auto` + `HERDR_WEB_HOST=0.0.0.0`，
也就是上面的 **B 档**），直连口就是主口，这条路自动就有。

真证书那档（`TLS_CERT` / `ACME_DNS`）**必须**另开这个口：那张证书只对域名有效，照 IP 直连
必然 SAN 不匹配，而 Let's Encrypt 不给私网 IP 签证书。
