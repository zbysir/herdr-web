# herdr-web

浏览器里的 herdr 终端。项目怎么跑、环境变量、移动端手势、已处理的坑：[README.md](README.md)。

正在做**语音投稿**——平板手写笔说话打字、框选重说改字，投进 herdr 的 agent pane。动 herdr socket 调用、输入框抽取、或发件箱 UI 之前先读 [HANDOFF.md](HANDOFF.md)：里面是已验证的 API 语义、按 agent 抽输入框的做法、和三个会让你静默拿到空字符串的坑。
